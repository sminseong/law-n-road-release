<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lawnroad.board.mapper.CommentMapper">

    <!-- 답변 등록 -->
    <insert id="insertAnswer" parameterType="com.lawnroad.board.dto.AnswerRegisterDto">
        INSERT INTO comment (board_no, user_no, content)
        VALUES (#{boardNo}, #{userNo}, #{content})
    </insert>

<!--    &lt;!&ndash; 게시글별 답변 목록 조회 &ndash;&gt;-->
<!--    <select id="selectAnswersByBoardNo" resultType="com.lawnroad.board.dto.AnswerResponseDto">-->
<!--        SELECT-->
<!--            l.name AS lawyerName,-->
<!--            l.profile AS lawyerProfileImage,-->
<!--            c.content,-->
<!--            c.created_at-->
<!--        FROM comment c-->
<!--                 JOIN lawyer l ON c.user_no = l.no-->
<!--        WHERE c.board_no = #{boardNo}-->
<!--        ORDER BY c.created_at ASC-->
<!--    </select>-->

</mapper>